I'm looking at the C code generated by Ghidra. My first step is always to find the `main` function. Here, it appears to be `FUN_0010279c`.

<img width="1813" height="786" alt="screenshot-1760274387" src="https://github.com/user-attachments/assets/c708e98c-af8d-4d15-88c8-0e0e22bf2485" />




Inside this function, I see the `printf` and `puts` calls that print the welcome banner. Then, my eyes are drawn to these lines:

```c
undefined8 local_118 [33];
...
// The buffer is zeroed out here
...
FUN_00101a97(local_118);
```
 A large local buffer (`local_118`) is created on the stack. This buffer is almost certainly the entire state of the Yan85 virtual machine – its RAM and its registers all in one contiguous block of memory. This buffer is then passed to `FUN_00101a97`, which I'll bet is the main logic of the emulated program

My immediate goal is to figure out the structure of this "VM State" buffer.



I need to understand how the emulator interacts with the `local_118` buffer. I'll start by looking for functions that look like they read and write to this state. I start looking at the functions called by `FUN_00101a97`.



Then i found sm interesting functions : `FUN_00101363` and `FUN_00101415`.

*   `FUN_00101363` takes the VM state (`param_1`) and a character (`param_2`) and *returns* a value. This looks like a "get register" function.
*   `FUN_00101415` takes the VM state, a character, and a value (`param_3`) and has no return. This must be a "set register" function.

 looking  inside `FUN_00101415` (the setter):

```c
void FUN_00101415(long param_1, char param_2, undefined1 param_3) {
  if (param_2 == '\x10') {         // The character '\x10'
    *(param_1 + 0x100) = param_3; // accesses offset 0x100 in the VM state
  }
  else if (param_2 == '@') {      // The character '@' (0x40)
    *(param_1 + 0x101) = param_3; // accesses offset 0x101
  }
  else if (param_2 == ' ') {      // The character ' ' (0x20)
    *(param_1 + 0x102) = param_3; // accesses offset 0x102
  }
  // ...and so on...
}
```

 The second parameter is a unique ID for each register, and the location it writes to is its place in the VM State buffer. I can now build a complete map.

| Register ID | Hex | VM State Offset | My Nickname/Purpose |
| :--- | :--- | :--- | :--- |
| `'\x10'` | 0x10 | `+0x100` | REG\_A (General purpose) |
| `'@'` | 0x40 | `+0x101` | REG\_B (Often used as an address pointer) |
| `' '` | 0x20 | `+0x102` | REG\_C (Often used as a counter) |
| `'\x01'` | 0x01 | `+0x103` | REG\_D (General purpose) |
| `'\x04'` | 0x04 | `+0x104` | REG\_SP (Stack Pointer, based on push/pop logic) |
| `'\x02'` | 0x02 | `+0x105` | REG\_IP (Instruction Pointer, not used in a loop here) |
| `'\b'` | 0x08 | `+0x106` | REG\_FLAGS (Status flags for comparisons) |

**Finding the Memory:**

Two other simple functions, `FUN_001014e6` (read memory) and `FUN_00101507` (write memory), confirm my hypothesis about the RAM (chip 8 has the same thingy)

```c
// This function just reads a byte from the VM state at a given offset.
undefined1 FUN_001014e6(long param_1, byte param_2) {
  return *(undefined1 *)(param_1 + (int)(uint)param_2);
}
```
This confirms that the first `0x100` (256) bytes of the VM State buffer act as the Yan85's RAM.



Now that I know the machine's components, I can figure out what the instructions do. The C functions *are* the instructions. I'll go through the key ones and give them assembly-like mnemonics.

*   `FUN_00101533`: Calls the register setter. It moves a constant value into a register. I'll name this **`MOV reg, imm`**.
*   `FUN_00101568`: Gets two registers, adds their values, and stores the result in the first one. This is clearly **`ADD dest_reg, src_reg`**.
*   `FUN_00101687`: Takes a destination address from one register and a value from another, and performs a memory write. This is **`STORE [addr_reg], val_reg`**.
*   `FUN_00101896`: This one is complex. It checks various bits of its second argument and calls system functions like `read`, `write`, and `exit`. The registers are used as arguments. This is the **`SYSCALL`** instruction. Specifically, the bitmask `0x10` triggers a `read` operation.



 analyzing  the main event: `FUN_00101a97`. I'll read it from top to bottom, translating the C calls into my Yan85 assembly mnemonics.

**Phase 1: Reading My Input**

The function begins with:
```c
// My Translation:
// MOV REG_B, 0x50       ; Set memory buffer address to 0x50 for input
FUN_00101533(param_1,0x40,0x50);
// MOV REG_C, 6          ; Set number of bytes to read to 6
FUN_00101533(param_1,0x20,6);
// MOV REG_A, 0          ; Set file descriptor to 0 (stdin)
FUN_00101533(param_1,0x10,0);
// SYSCALL read(fd=REG_A, buf=REG_B, count=REG_C)
FUN_00101896(param_1,0x10,0x10);
```
**Conclusion:** The very first thing the program does is wait for me to provide exactly **6 bytes** of input. It stores these 6 bytes in its own memory, starting at address `0x50`.


Next, I see a long, repetitive block of code.
```c
// My Translation:
// MOV REG_B, 0x70       ; Set a pointer to memory address 0x70
FUN_00101533(param_1,0x40,0x70);
// MOV REG_C, 1          ; Set a counter to 1 (to increment the pointer)
FUN_00101533(param_1,0x20,1);

// --- First byte ---
// MOV REG_A, 0x80       ; Load the byte 0x80 into REG_A
FUN_00101533(param_1,0x10,0x80);
// STORE [REG_B], REG_A  ; Store 0x80 at address 0x70
FUN_00101687(param_1,0x40,0x10);
// ADD REG_B, REG_C      ; Increment the memory pointer to 0x71
FUN_00101568(param_1,0x40,0x20);

// --- Second byte ---
// MOV REG_A, 0x3c       ; Load the byte 0x3c
FUN_00101533(param_1,0x10,0x3c);
// STORE [REG_B], REG_A  ; Store 0x3c at address 0x71
FUN_00101687(param_1,0x40,0x10);
// ...and so on
```
I can see the pattern. It's writing a sequence of constant bytes into memory starting at `0x70`. I'll just extract the constants it's writing: `0x80`, `0x3c`, `0x42`, `0xf9`, `0x51`, `0x51`.

 The program builds a 6-byte secret password in its memory at address `0x70`. The secret is: `\x80\x3c\x42\xf9\x51\x51`.



The final piece of the puzzle is the `memcmp` call.
```c
iVar1 = memcmp((void *)(param_1 + 0x70), (void *)(param_1 + 0x50), 6);
if (iVar1 == 0) {
  // CORRECT!
}
else {
  // INCORRECT!
}
```
It's comparing the memory region at `0x70` (the secret it just built) with the memory region at `0x50` (where it stored my input). If they are identical, i get the flag 



 I need to send the exact 6-byte sequence `\x80\x3c\x42\xf9\x51\x51` as input to the program.



```bash
python -c 'import sys; sys.stdout.buffer.write(b"\x80\x3c\x42\xf9\x51\x51")'
```

<img width="1921" height="1081" alt="screenshot-1760274401" src="https://github.com/user-attachments/assets/3ae1ef27-07fd-4aae-9ff7-c05ba1b0bcf7" />






